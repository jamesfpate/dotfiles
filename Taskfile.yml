# https://taskfile.dev

version: '3'

tasks:
  stow:
    desc: update symlinks with stow
    cmds:
      - cd symlinks && touch test && git restore . && cd ..
      #- stow --restow --adopt --target ~ --verbose --no-folding */
